spring.application.name=ConfigServer

server.port=8888

#point the config server to the git repository

spring.cloud.config.server.git.uri=${HOME}/Desktop/trial

#spring.cloud.config.server.git.uri=https://github.com/gsoni100/MyProjects
#spring.cloud.config.server.native.searchLocations=file://${HOME}/Desktop/configfile
#SPRING_PROFILES_ACTIVE=native

#Disable security of the Management endpoint
management.security.enabled=false


#connect it to eureka and refresh every 5 seconds
eureka.client.registryFetchIntervalSeconds = 5
eureka.client.service-url.default-zone=http://localhost:8761/eureka
